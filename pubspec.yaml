name: trending_on_github
description: Trending Repos On Github project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3


  #Service Locator
  get_it: ^7.2.0

  #UI Layer
  flutter_bloc: ^7.2.0
  email_validator: ^2.0.1

  #Domain
  dartz: ^0.10.0
  equatable: ^2.0.3

  #Data
  connectivity_plus: ^1.1.0

  #Data - Remote
  url_launcher: ^6.0.11
  dio: ^4.0.0
  firebase_core: ^1.6.0
  firebase_auth: ^3.1.1

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/